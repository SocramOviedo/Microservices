package com.ApiCuentaMovimiento.ApiCuentaMovimiento.services;

//import java.util.Map;

//import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.stereotype.Service;

@Service
public class CuentaMovimientoService {
    /*
     * @RabbitListener(queues = "mi-queue")
     * public void procesarMensaje(Map<String, Object> mensaje) {
     * String tipo = (String) mensaje.get("tipo");
     * if ("NuevaPersona".equals(tipo)) {
     * // Procesar la nueva persona
     * Map<String, Object> datos = (Map<String, Object>) mensaje.get("datos");
     * Long id = (Long) datos.get("id");
     * String nombre = (String) datos.get("nombre");
     * // Realizar las acciones necesarias
     * }
     * }
     * 
     */
}